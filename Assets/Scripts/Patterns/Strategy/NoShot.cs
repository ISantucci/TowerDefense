/// <summary>Sin comportamiento de combate (Spawner / Support / Trap / Pull todavía no implementados).</summary>
public class NoShot : IShootStrategy
{
    public void Shoot(ShootContext ctx)
    {
        // Intencionalmente vacío.
    }
}
